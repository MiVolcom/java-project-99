spring:
  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: create
  output:
    ansi:
      enabled: always
  profiles:
    default: development
logging:
  level:
    root: INFO
rsa:
  private-key: classpath:certs/private.pem
  public-key: classpath:certs/public.pem

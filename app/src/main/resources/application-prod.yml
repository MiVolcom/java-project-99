spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${JDBC_DATABASE_URL}
    username: ${Username}
    password: ${Password}
  jpa:
    properties:
      hibernate:
        format_sql: true